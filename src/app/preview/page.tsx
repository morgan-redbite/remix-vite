export default function Preview() {
  // const { data, error } = useSWR('/api/preview', fetcher);

  // if (error) return <div>Failed to load</div>;
  // if (!data) return <div>Loading...</div>;

  return (
    <div>
      <h1>Preview</h1>
      {/* <p>{data.message}</p> */}
    </div>
  );
}